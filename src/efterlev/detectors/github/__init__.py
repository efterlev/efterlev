"""GitHub-source detector cohort. Each detector reads `.github/workflows/`
or related repo-metadata files. Imports trigger registration via
`@detector` — see `efterlev/detectors/__init__.py` for the loader.
"""
